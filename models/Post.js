const mongoose = require("mongoose");
const Schema = mongoose.Schema;

//Create schema / Создаем схему
const PostSchema = new Schema({
  user: {
    type: Schema.Types.ObjectId,
    ref: "users"
  },
  text: {
    type: String,
    required: true
  },
  name: {
    type: String
  },
  avatar: {
    type: String
  },
  likes: [
    {
      user: {
        type: Schema.Types.ObjectId,
        ref: "users"
      }
    }
  ],
  comments: [
    {
      user: {
        type: Schema.Types.ObjectId,
        ref: "users"
      },
      text: {
        type: String,
        required: true
      },
      name: {
        type: String
      },
      avatar: {
        type: String
      },
      date: {
        type: Date,
        dafault: Date.now
      }
    }
  ],
  date: {
    type: Date,
    dafault: Date.now
  }
});

module.exports = Post = mongoose.model("post", PostSchema);
